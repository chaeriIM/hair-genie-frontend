import React from 'react'

const LoginPage = () => {
  return (
    <div>
      <div className='wrapper'>
        <img
          src="/images/logo.svg"
          alt="logo"
          onClick={() => (window.location.href = "/")}
          className='logo'
        />
      </div>

      LoginPage</div>
  )
}

export default LoginPage